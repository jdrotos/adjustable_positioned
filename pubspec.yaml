name: adjustable_positioned
description: A Positioned Widget that can handle repositioning itself based on user input.
version: 1.0.0
homepage: https://github.com/jdrotos/adjustable_positioned
repository: https://github.com/jdrotos/adjustable_positioned

environment:
  sdk: ^3.2.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# The following section is specific to Flutter packages.
flutter:
